/**
 * Interview Service
 * Business logic for interview functionality
 */

class InterviewService {
    /**
     * Placeholder for future interview service methods
     * Most interview logic is handled in the WebSocket server
     */

    static async saveInterviewSession(userId, sessionData) {
        // TODO: Save to database when needed
        console.log('Saving interview session for user:', userId);
    }

    static async getInterviewHistory(userId) {
        // TODO: Retrieve from database when needed
        return [];
    }
}

module.exports = InterviewService;
